# Copy this to .env and fill in your values

# Required: OpenAI API key — used for both gpt-4o model calls and text-embedding-3-small
OPENAI_API_KEY=sk-...

# Set automatically by docker-compose — only override if running outside Docker
# DATABASE_URL=postgresql+asyncpg://copilot:copilot@localhost/copilot
